1. Текст запроса без дополнительных индексов;
2. План выполнения с использованием EXPLAIN ANALYZE;
3. Анализ узких мест выполнения;
4. Гипотеза об оптимизации запроса, обоснование выбранного индекса;
5. Созданный индекс;
6. Результаты повторного выполнения запроса с индексом;
7. Сравнение результата;
8. Вывод о корректности гипотезы

### Сложный фильтр

1. `SELECT * FROM receipt_item`
   `WHERE product_id = 25`
   `AND purchase_price BETWEEN 100 AND 500`

2. Gather с двумя рабочими процессами,
   внутри каждого – Parallel Seq Scan на таблице receipt_item.
   Фактические временные показатели:
   общее время выполнения 39.250 мс,
   максимальное время среди параллельных процессов 36.215 мс,
   при этом каждый процесс отфильтровал в среднем 333367 строк,
   а всего строк возвращено 400.
   Буферы: shared hit=47, read=7309.
   План целиком:
   `Gather (cost=1000.00..15691.21 rows=399 width=24)`
   `(actual time=0.190..39.225 rows=400 loops=1)`
   `-> Parallel Seq Scan (cost=0.00..14651.31 rows=166 width=24)`
   `(actual time=0.489..36.215 rows=133 loops=3)`
   `Filter: ((purchase_price >= '100'::numeric)`
   `AND (purchase_price <= '500'::numeric)`
   `AND (product_id = 25))`
   `Rows Removed by Filter: 333367`

3. Полное последовательное сканирование всей таблицы (Seq Scan),
   несмотря на параллелизацию.
   Из миллиона строк условиям удовлетворяют только около 400,
   поэтому 99,96% данных отбрасываются фильтром,
   что приводит к огромным накладным расходам на чтение.
   Операции ввода-вывода: прочитано 7309 блоков с диска и 47 из кэша,
   то есть практически вся таблица.
   Время 39.250 мс для миллиона строк,
   при росте объёма данных время будет расти линейно.

4. Создать составной индекс по полям `(product_id, purchase_price)`.
   Первое поле – точное сравнение, второе – диапазон.
   Такой индекс позволит выполнить Index Scan (или Bitmap Index Scan)
   и найти только те строки, где `product_id = 25`,
   а затем среди них быстро отобрать записи
   с `purchase_price` от 100 до 500,
   полностью избежав чтения всей таблицы.
   B-дерево выбрано, потому что оно наилучшим образом поддерживает
   оба типа условий, присутствующих в запросе:
   точное сравнение по полю `product_id` и диапазон по полю `purchase_price`.
   B-дерево хранит данные в отсортированном виде,
   что позволяет после нахождения первой записи с `product_id = 25`
   продолжить последовательное чтение только тех записей,
   у которых `purchase_price` попадает в интервал от 100 до 500.
   При этом сложность поиска составляет O(log N) для нахождения начальной точки,
   а затем чтение результата линейно пропорционально
   количеству выбранных строк, а не размеру всей таблицы.
   Альтернативные типы индексов не подходят.
   Хеш-индекс поддерживает только сравнение на равенство, но не диапазоны.
   GIN-индекс предназначен для полнотекстового поиска
   и работы с массивами или JSONB, он избыточен и неэффективен
   для числового диапазона.
   GiST-индекс мог бы работать для диапазонов,
   но для простых чисел B-дерево быстрее и компактнее.
   BRIN-индекс подходит для очень больших таблиц,
   где значения физически коррелированы с порядком строк,
   но наш запрос требует точечного поиска по конкретному `product_id` –
   такой индекс пришлось бы сканировать полностью или почти полностью.
   B-дерево же обеспечивает надёжную производительность
   для комбинации равенства и интервала.

5. `CREATE INDEX idx_receipt_item_product_price`
   `ON receipt_item (product_id, purchase_price)`

6. Bitmap Heap Scan на receipt_item с условием индекса,
   и внутри него Bitmap Index Scan на созданном индексе.
   Фактическое время выполнения сократилось до 2.651 мс.
   Детали плана:
   `Bitmap Heap Scan (cost=13.51..1308.13 rows=399 width=24)`
   `(actual time=0.137..2.543 rows=400 loops=1)`
   `Recheck Cond: ((product_id = 25)`
   `AND (purchase_price >= '100'::numeric)`
   `AND (purchase_price <= '500'::numeric))`
   `Heap Blocks: exact=388`
   `Buffers: shared hit=13 read=379`
   `-> Bitmap Index Scan on idx_receipt_item_product_price`
   `(cost=0.00..13.41 rows=399 width=0)`
   `(actual time=0.099..0.099 rows=400 loops=1)`
   `Index Cond: ((product_id = 25)`
   `AND (purchase_price >= '100'::numeric)`
   `AND (purchase_price <= '500'::numeric))`
   `Buffers: shared read=4`
   Время планирования 0.249 мс.

7. Без индекса время выполнения 39.250 мс,
   с индексом – 2.651 мс,
   ускорение примерно в 14,8 раза.
   Количество прочитанных буферов сократилось с 7356 (47+7309)
   до 392 (13+379), то есть в 18,8 раза.
   Вместо параллельного последовательного сканирования всей таблицы
   теперь используется битовое сканирование индекса,
   которое обращается только к 388 блокам кучи (Heap Blocks)
   и 4 блокам индекса.
   Количество возвращённых строк совпадает (400),
   точность фильтрации достигнута без лишних операций.

8. Гипотеза полностью подтверждена.
   Составной индекс `(product_id, purchase_price)`
   эффективно устранил узкое место полного сканирования таблицы.
   Он оптимален для запросов, содержащих условие равенства
   по первому столбцу и диапазон по второму.
   Рекомендуется создавать такие индексы
   для часто выполняемых аналитических запросов
   с комбинированными фильтрами.

### Сортировка с ограничением (ORDER BY + LIMIT)

1. `SELECT * FROM receipt_item ORDER BY purchase_price DESC LIMIT 10`

2. План без индекса:
   `Limit (cost=21533.29..21534.46 rows=10 width=24)`
   `(actual time=180.797..182.095 rows=10 loops=1)`
   `  Buffers: shared hit=654 read=6774`
   `  -> Gather Merge (cost=21533.29..118810.92 rows=833750 width=24)`
   `      (actual time=180.795..182.092 rows=10 loops=1)`
   `        Workers Planned: 2, Workers Launched: 2`
   `        Buffers: shared hit=654 read=6774`
   `        -> Sort (cost=20533.27..21575.46 rows=416875 width=24)`
   `            (actual time=177.449..177.450 rows=8 loops=3)`
   `              Sort Key: purchase_price DESC`
   `              Sort Method: top-N heapsort Memory: 26kB`
   `              Buffers: shared hit=654 read=6774`
   `              -> Parallel Seq Scan on receipt_item`
   `                  (cost=0.00..11524.75 rows=416875 width=24)`
   `                  (actual time=0.091..146.573 rows=333500 loops=3)`
   `                    Buffers: shared hit=582 read=6774`
   `Planning Time: 0.082 ms`
   `Execution Time: 182.110 ms`

3. Полное последовательное сканирование всей таблицы (1 000 000 строк)
   с параллельной обработкой, затем сортировка всех строк по purchase_price DESC.
   Для получения всего 10 строк пришлось обработать миллион записей.
   Сортировка методом top-N heapsort выполняется в памяти,
   но требует чтения всех строк каждым воркером (~333 500 строк на воркер).
   Время выполнения 182.1 мс, из них 146.6 мс – сканирование,
   остальное – сортировка и слияние.
   Операции ввода-вывода: 654 блока из кэша и 6774 с диска – практически вся таблица.

4. Создать индекс на поле purchase_price (по убыванию).
   Индекс B-дерево поддерживает сортировку,
   поэтому запрос с ORDER BY purchase_price DESC LIMIT 10
   может выполнить Index Scan в обратном порядке,
   взяв первые 10 записей из индекса без чтения всей таблицы.
   Так как индекс содержит отсортированные значения,
   PostgreSQL сможет прочитать только первые 10 записей индекса,
   а затем для каждой из них обратиться к таблице.
   Сложность станет O(log N + k), где k = 10,
   что кардинально быстрее текущего плана.
   B-дерево выбрано, потому что оно хранит данные в отсортированном порядке
   и поддерживает как возрастающую, так и убывающую сортировку.
   Альтернативы (хеш, GIN, GiST) не подходят,
   так как не обеспечивают упорядоченный вывод.

5. Создан индекс:
   `CREATE INDEX idx_receipt_item_price_desc`
   `ON receipt_item (purchase_price DESC)`

6. Повторное выполнение запроса с индексом:
   `Limit (cost=0.42..0.98 rows=10 width=24)`
   `(actual time=0.050..0.118 rows=10 loops=1)`
   `  Buffers: shared hit=2 read=11`
   `  -> Index Scan using idx_receipt_item_price_desc on receipt_item`
   `      (cost=0.42..55419.92 rows=1000500 width=24)`
   `      (actual time=0.049..0.115 rows=10 loops=1)`
   `        Buffers: shared hit=2 read=11`
   `Planning Time: 0.204 ms`
   `Execution Time: 0.131 ms`

7. Без индекса время выполнения 182.110 мс,
   с индексом – 0.131 мс,
   ускорение примерно в 1390 раз.
   Количество прочитанных буферов сократилось с 7428 (654+6774)
   до 13 (2+11), то есть в 571 раз.
   Вместо параллельного последовательного сканирования всей таблицы
   и сортировки миллиона строк теперь выполняется Index Scan,
   который считывает только 10 записей из индекса
   и делает соответствующее количество обращений к таблице.
   Количество возвращённых строк совпадает (10).

8. Гипотеза полностью подтверждена.
   Индекс на поле сортировки (особенно с указанием направления DESC)
   радикально ускоряет запросы с ORDER BY и LIMIT.
   Без индекса база данных вынуждена сканировать и сортировать все строки,
   что приводит к огромным накладным расходам.
   Для запросов, которые часто извлекают «топ N» записей,
   индекс на соответствующем поле обязателен.

### Альтернативные варианты индексирования (два различных индекса)

1. `SELECT * FROM receipt_item`
   `WHERE product_id = 25`
   `AND quantity > 10`

2. План без индекса:
   `Gather (cost=1000.00..13406.53 rows=1312 width=48)`
   `(actual time=0.236..52.635 rows=9134 loops=1)`
   `  Workers Planned: 2, Workers Launched: 2`
   `  Buffers: shared read=7356 dirtied=7356 written=7260`
   `  -> Parallel Seq Scan on receipt_item`
   `      (cost=0.00..12275.33 rows=547 width=48)`
   `      (actual time=0.030..49.393 rows=3045 loops=3)`
   `        Filter: ((quantity > '10'::numeric) AND (product_id = 25))`
   `        Rows Removed by Filter: 330455`
   `        Buffers: shared read=7356 dirtied=7356 written=7260`
   `Planning Time: 0.211 ms`
   `Execution Time: 53.053 ms`

3. Полное последовательное сканирование всей таблицы (1 000 000 строк)
   с параллельной обработкой. Фильтр отбрасывает около 99,1% строк,
   так как product_id = 25 встречается примерно в 1% записей,
   а quantity > 10 – ещё в части из них.
   Фактически подходит ~9134 строки, но прочитаны все миллион.
   Время 53.053 мс, буферов прочитано 7356 с диска.
   При росте данных время будет расти линейно.

4. Для данного запроса можно предложить два различных варианта индекса:
   - Индекс A: `(product_id, quantity)` – первым идёт поле точного равенства,
     затем поле диапазона.
   - Индекс B: `(quantity, product_id)` – первым идёт поле диапазона,
     затем поле точного равенства.
   Гипотеза: индекс A будет эффективнее,
   так как сначала сужает выборку по `product_id` (редкое значение),
   а внутри этого сегмента уже отсортированы `quantity`,
   что позволяет быстро применить условие `> 10`.
   Индекс B сначала выберет все строки с `quantity > 10` (около 500 000 строк),
   а затем среди них будет искать `product_id = 25` – это потребует
   сканирования гораздо большего количества записей индекса.
   Оба индекса – B-деревья, так как поддерживают и равенство, и диапазон.

5. Созданы два индекса (по очереди, каждый тестировался отдельно):
   `CREATE INDEX idx_product_quantity ON receipt_item (product_id, quantity);`
   `CREATE INDEX idx_quantity_product ON receipt_item (quantity, product_id);`

6. Результаты повторного выполнения запроса:

   **С индексом A (product_id, quantity):**
   `Bitmap Heap Scan on receipt_item (cost=45.52..4036.51 rows=1668 width=48)`
   `(actual time=1.634..26.534 rows=9134 loops=1)`
   `  Recheck Cond: ((product_id = 25) AND (quantity > '10'::numeric))`
   `  Heap Blocks: exact=5255`
   `  Buffers: shared hit=108 read=5185`
   `  -> Bitmap Index Scan on idx_product_quantity`
   `      (cost=0.00..45.10 rows=1668 width=0)`
   `      (actual time=1.187..1.187 rows=9134 loops=1)`
   `        Index Cond: ((product_id = 25) AND (quantity > '10'::numeric))`
   `        Buffers: shared read=38`
   `Planning Time: 0.197 ms`
   `Execution Time: 26.954 ms`

   **С индексом B (quantity, product_id):**
   `Bitmap Heap Scan on receipt_item (cost=8475.84..12466.83 rows=1668 width=48)`
   `(actual time=31.705..34.806 rows=9090 loops=1)`
   `  Recheck Cond: ((quantity > '10'::numeric) AND (product_id = 25))`
   `  Heap Blocks: exact=5226`
   `  Buffers: shared hit=8679`
   `  -> Bitmap Index Scan on idx_quantity_product`
   `      (cost=0.00..8475.42 rows=1668 width=0)`
   `      (actual time=31.228..31.229 rows=9090 loops=1)`
   `        Index Cond: ((quantity > '10'::numeric) AND (product_id = 25))`
   `        Buffers: shared hit=3453`
   `Planning Time: 0.050 ms`
   `Execution Time: 35.052 ms`

7. Сравнение результатов:
   - Без индекса: 53.053 мс.
   - С индексом A: 26.954 мс – ускорение примерно в 1,97 раза.
   - С индексом B: 35.052 мс – ускорение примерно в 1,51 раза.
   Индекс A оказался быстрее индекса B на 23% (35.05 мс против 26.95 мс).
   Количество прочитанных буферов: для A – 5331 (108+5185+38), для B – 12132 (8679+3453).
   Индекс B прочитал почти в 2,3 раза больше блоков,
   потому что сначала было выбрано много строк по условию `quantity > 10`,
   и только затем отфильтровано `product_id = 25`.
   Индекс A выбрал сразу нужные 9134 строки.
   Обратите внимание: оба индекса дали ускорение, но индекс A – более эффективный.

8. Гипотеза подтверждена: индекс с полем равенства на первом месте
   эффективнее индекса с полем диапазона на первом месте,
   особенно когда условие равенства сильно селективно.
   Для запросов, содержащих условие `=` и условие диапазона,
   оптимальным является составной индекс, где сначала идёт столбец
   с точным сравнением, а затем столбец с диапазоном.
   Альтернативный индекс (сначала диапазон, потом равенство)
   даёт меньшее ускорение из-за необходимости сканировать
   значительно большую часть индекса.

### Текстовый поиск (подстрока, префикс и суффикс) – регистронезависимый, высокая селективность

1. `SELECT * FROM product WHERE LOWER(name) LIKE '%мульти%'`
   (в таблице 1 000 000 строк и только 10 записей содержат подстроку «мульти»)

2. План без индекса:
   `Gather (cost=1000.00..10008.10 rows=13 width=468)`
   `(actual time=109.810..111.242 rows=10 loops=1)`
   `  Workers Planned: 2, Workers Launched: 2`
   `  Buffers: shared read=8188 dirtied=8188 written=8092`
   `  -> Parallel Seq Scan on product (cost=0.00..9006.80 rows=5 width=468)`
   `      (actual time=107.728..107.730 rows=3 loops=3)`
   `        Filter: (lower((name)::text) ~~ '%мульти%'::text)`
   `        Rows Removed by Filter: 333367`
   `        Buffers: shared read=8188 dirtied=8188 written=8092`
   `Planning Time: 0.156 ms`
   `Execution Time: 111.257 ms`

3. Полное последовательное сканирование таблицы product (1 000 000 строк)
   с параллельной обработкой. Условие `LOWER(name) LIKE '%мульти%'` применяется к каждой строке,
   требуя приведения к нижнему регистру и проверки наличия подстроки.
   Несмотря на то, что подстроке удовлетворяют только 10 строк (селективность 0,001%),
   сканируются все строки. Время выполнения 111.3 мс, прочитано 8188 блоков с диска.
   Основное узкое место – полное сканирование таблицы при крайне низкой селективности.

4. Для оптимизации регистронезависимого поиска по редкой подстроке выбран GIN-индекс на выражении `LOWER(name)` с расширением `pg_trgm`, 
   поскольку условие `LOWER(name) LIKE '%мульти%'` содержит ведущий `%` и требует поиска произвольной подстроки.
   B‑tree и Hash‑индексы неприменимы (первый требует префикса, второй — только точного равенства), 
   а GiST с триграммами уступает GIN в скорости и компактности. Расширение `pg_trgm` разбивает текст на триграммы, 
   позволяя индексу быстро находить строки, содержащие все триграммы искомой подстроки, и затем проверять их точное совпадение.
   Гипотеза: для высокой селективности (редкое слово, например, «мульти») индекс позволит выполнить точечный `Bitmap Index Scan`,
   возвращающий всего несколько записей, после чего `Bitmap Heap Scan` прочитает только соответствующие блоки таблицы. 
   Это должно дать ускорение в сотни или тысячи раз по сравнению с полным последовательным сканированием.

5. Создан индекс:
   `CREATE EXTENSION IF NOT EXISTS pg_trgm;`
   `CREATE INDEX idx_product_lower_name_trgm ON product USING GIN (LOWER(name) gin_trgm_ops);`

6. Повторное выполнение запроса с индексом:
   `Bitmap Heap Scan on product (cost=68.78..437.12 rows=100 width=34)`
   `(actual time=0.025..0.030 rows=10 loops=1)`
   `  Recheck Cond: (lower((name)::text) ~~ '%мульти%'::text)`
   `  Heap Blocks: exact=1`
   `  Buffers: shared hit=10`
   `  -> Bitmap Index Scan on idx_product_lower_name_trgm (cost=0.00..68.75 rows=100 width=0)`
   `      (actual time=0.018..0.018 rows=10 loops=1)`
   `        Index Cond: (lower((name)::text) ~~ '%мульти%'::text)`
   `        Buffers: shared hit=9`
   `Planning Time: 0.215 ms`
   `Execution Time: 0.052 ms`

7. Сравнение результатов:
   - Без индекса: 111.257 мс, прочитано 8188 блоков.
   - С индексом: 0.052 мс, прочитано 10 буферов (9 из индекса + 1 из кучи).
   Ускорение примерно в **2140 раз** (111.257 / 0.052 ≈ 2140).
   Количество операций ввода-вывода сократилось в 818 раз (8188 / 10 ≈ 818).
   Индекс позволил найти 10 нужных строк, обратившись только к 9 блокам индекса
   и одному блоку таблицы (так как все 10 строк могли находиться в одном блоке кучи).

8. Вывод: гипотеза полностью подтверждена. Для запросов с высокой селективностью
   (очень редкая подстрока) GIN-индекс на `LOWER(name)` с триграммами даёт
   колоссальное ускорение – более чем в 2000 раз. Эффективность индекса
   критически зависит от доли возвращаемых строк: при 5% строк ускорение
   составляет ~2 раза, при 0,001% – тысячи раз. Таким образом, текстовый поиск
   по подстроке необходимо сопровождать индексом `pg_trgm`, особенно если
   искомые значения встречаются редко.

### Соединение таблиц (JOIN)

1. Оптимизированный запрос (после изменения структуры):
```sql
SELECT ri.id, ri.quantity, ri.purchase_price, p.name, p.unit
FROM (
    SELECT id, quantity, purchase_price, product_id
    FROM receipt_item
    ORDER BY purchase_price DESC
    LIMIT 1000
) ri
JOIN product p ON ri.product_id = p.id
WHERE p.category_id = 3
ORDER BY ri.purchase_price DESC
LIMIT 100;

2. План выполнения без индекса на receipt_item.purchase_price:
Limit (cost=29617.41..37337.19 rows=5 width=496) (actual time=76.438..77.778 rows=100 loops=1)
Buffers: shared hit=1702 read=7258
-> Nested Loop (cost=29617.41..37337.19 rows=5 width=496) (actual time=76.436..77.772 rows=100 loops=1)
Buffers: shared hit=1702 read=7258
-> Limit (cost=29616.99..29733.67 rows=1000 width=44) (actual time=76.398..77.418 rows=383 loops=1)
Buffers: shared hit=170 read=7258
-> Gather Merge (cost=29616.99..106145.17 rows=655910 width=44) (actual time=76.397..77.399 rows=383 loops=1)
Workers Planned: 2, Workers Launched: 2
Buffers: shared hit=170 read=7258
-> Sort (cost=28616.97..29436.86 rows=327955 width=44) (actual time=74.216..74.242 rows=710 loops=3)
Sort Key: receipt_item.purchase_price DESC
Sort Method: top-N heapsort Memory: 195kB
-> Parallel Seq Scan on receipt_item (cost=0.00..10635.55 rows=327955 width=44) (actual time=0.022..29.399 rows=333500 loops=3)
Buffers: shared hit=98 read=7258
-> Index Scan using product_pkey on product p (cost=0.42..7.59 rows=1 width=460) (actual time=0.001..0.001 rows=0 loops=383)
Index Cond: (id = receipt_item.product_id)
Filter: (category_id = 3)
Rows Removed by Filter: 1
Buffers: shared hit=1532
Planning Time: 0.542 ms
Execution Time: 77.851 ms

3. Основное узкое место - получение топ-1000 записей по purchase_price. Без индекса на поле сортировки планировщик вынужден выполнить параллельное последовательное сканирование всей таблицы receipt_item (1 000 000 строк) и отсортировать все строки (даже с top-N heapsort это требует чтения и обработки каждого блока). Время выполнения ~77.8 мс, чтение 7258 буферов с диска.

4. Для ускорения операции ORDER BY purchase_price DESC LIMIT N необходимо создать B‑tree индекс по полю purchase_price в убывающем порядке. B‑tree хранит данные в отсортированной структуре, поэтому PostgreSQL может выполнить Index Scan в обратном порядке, последовательно считывая только первые N записей индекса и обращаясь к таблице только для этих строк. Это полностью исключает полное сканирование и сортировку. Индекс на (purchase_price DESC) выбран потому, что направление сортировки в запросе – DESC, а B‑tree позволяет эффективно читать как в прямом, так и в обратном порядке, но явное указание DESC может дать небольшое преимущество. Альтернативы (хеш, GIN) не поддерживают сортировку.

5. CREATE INDEX idx_receipt_item_purchase_price ON receipt_item (purchase_price DESC);

6. Повторное выполнение оптимизированного запроса с индексом:
Limit (cost=0.84..7659.34 rows=5 width=496) (actual time=0.129..4.299 rows=100 loops=1)
Buffers: shared hit=1555 read=364
-> Nested Loop (cost=0.84..7659.34 rows=5 width=496) (actual time=0.128..4.289 rows=100 loops=1)
Buffers: shared hit=1555 read=364
-> Limit (cost=0.42..55.82 rows=1000 width=44) (actual time=0.056..3.335 rows=383 loops=1)
Buffers: shared hit=23 read=364
-> Index Scan using idx_receipt_item_purchase_price on receipt_item (cost=0.42..55419.93 rows=1000500 width=44) (actual time=0.055..3.284 rows=383 loops=1)
Buffers: shared hit=23 read=364
-> Index Scan using product_pkey on product p (cost=0.42..7.59 rows=1 width=460) (actual time=0.002..0.002 rows=0 loops=383)
Index Cond: (id = receipt_item.product_id)
Filter: (category_id = 3)
Rows Removed by Filter: 1
Buffers: shared hit=1532
Planning Time: 0.257 ms
Execution Time: 4.330 ms

7.
Без индекса: 77.851 мс, прочитано 8960 буферов (1702 hit + 7258 read).

С индексом: 4.330 мс, прочитано 1919 буферов (1555 hit + 364 read).
Ускорение примерно в 18 раз (77.851 / 4.330 ≈ 18.0).
Количество операций ввода-вывода сократилось в 4,7 раза (8960 / 1919 ≈ 4,7).
Вместо параллельного последовательного сканирования всей таблицы внутренний подзапрос выполнил Index Scan, который прочитал только 383 записи (столько реально потребовалось для получения 1000, но из-за фильтрации по категории фактически выбрано 383) и сделал минимальное количество обращений к таблице.

8. Вывод: гипотеза полностью подтверждена. Индекс на purchase_price DESC в сочетании с переписанным запросом (подзапрос с ORDER BY и LIMIT до соединения) дал ускорение в 18 раз. Ключевой фактор успеха – изменение структуры запроса, позволяющее применить индекс для получения топ-N до выполнения соединения и фильтрации. Если бы индексировалось только поле product_id или category_id, эффект был бы минимальным, так как основная тяжесть запроса – сортировка и ограничение. Данный пример показывает важность выбора правильного индекса и соответствующей перестройки запроса для его эффективного использования.